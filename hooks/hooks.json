{
  "description": "On first session after install, auto-wire cc-cream's status bar when the slot is free (else point to /cc-cream:setup).",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/auto-setup.js\""
          }
        ]
      }
    ]
  }
}
